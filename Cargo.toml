[package]
name = "farm"
version = "0.1.0"
edition = "2024"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = { version = "0.19.0", features = ["wayland"] }
bevy-inspector-egui = { version = "0.37.0" }
